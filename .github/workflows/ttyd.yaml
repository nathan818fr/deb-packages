name: ttyd

on:
  workflow_dispatch: {}
  schedule:
    - cron: '20 18 * * *'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./packages/ttyd.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
